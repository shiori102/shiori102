@IsTest
public class TestContactTrigger {
    @IsTest static void TestContactName(){
        Contact cont = new Contact(FirstName=null);
        insert cont;
        
        Test.startTest();
        Database.SaveResult result = Database.insert(cont, false);
        Test.stopTest();
        
        System.assert(!result.isSuccess());
    }
}