trigger ContactTrigger on Contact (before insert, before update) {
    for(Contact c : Trigger.New){
        if(c.FirstName ==null){
            c.addError('Please provide your first name.');
        }
    }
}